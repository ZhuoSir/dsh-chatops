# dsh-wechat 插件技术方案（个人号 Bot 版）

> 目标：用**普通微信（个人号）**扫码登录 Bot，之后在手机微信里直接发消息，即可操作 DSH 的所有工作区（workspace）和会话（session），并接收任务完成通知、完成远程审批。
>
> ⚠️ **风险前置声明**：个人微信没有官方 Bot API，本方案基于 wechaty 等第三方协议实现，**违反《微信软件许可及服务协议》，存在账号被限制登录/封号的真实风险**。请务必使用可承受损失的账号（建议小号），本插件定位为**个人自用工具**。

---

## 1. 交互模式（"扫码"在这个方案里是什么）

```
扫码（一次性）:  DSH GUI 插件页/终端显示登录二维码
                 → 用户用手机微信扫码 → Bot 账号登录上线（长期保持）

之后日常操控:    用户在任意设备的微信里，给以下任一聊天窗口发消息：
                 · 文件传输助手（最推荐：天然只有自己可见，零社交污染）
                 · 自己与自己的对话 / 指定联系人的私聊
                 · 指定群聊（@Bot 触发，适合小团队共用一个 DSH）
```

体验优势：不需要关注任何公众号，没有 5 秒被动回复/48 小时窗口/模板消息格式等一切公众平台限制——**收消息、发消息、发图片文件、拉群，全自由**，这是个人号路线唯一的、也是决定性的优势。

---

## 2. 协议层（Puppet）选型

wechaty 是框架，真正连微信的是 puppet（协议实现）。按现实可用性排序：

| Puppet | 协议 | 登录成功率 | 稳定性 | 成本 | 平台 | 结论 |
|---|---|---|---|---|---|---|
| **wechaty-puppet-padlocal** | iPad 协议 | 高（新号也可） | 高，维护活跃 | 付费 token（约 ¥99/月量级，按官方定价） | 全平台 | **推荐默认**：要稳定就付这个钱 |
| wechaty-puppet-wechat4u | 网页协议 | **低**（近年注册/未开通网页版的号直接拒登） | 中 | 免费 | 全平台 | 零成本首选，能登上就是赚 |
| wechaty-puppet-xp | Windows PC 注入 | 高 | 中（锁 PC 微信版本） | 免费 | **仅 Windows** | Windows 用户的免费稳定解 |
| gewechat / 其他第三方 | iPad/MAC 协议变种 | 高 | 视项目维护情况 | 部分免费 | 全平台 | 备选，注意项目存活度 |

**插件设计：puppet 做成配置项**（`puppet: padlocal | wechat4u | xp`），协议层可以随时换——这类项目生命周期短，抽象隔离是刚需。

> 登录二维码获取：wechaty 的 `scan` 事件吐出二维码 URL，插件渲染到 DSH GUI 插件页（同时输出终端二维码兜底）。首次登录后缓存登录态（memory card / 协议 token），重启免扫码；掉线再扫。

---

## 3. 总体架构

```
┌─────────────┐   普通聊天消息    ┌──────────────────┐
│  用户微信    │ ◄──────────────► │  微信服务器        │
│ (手机/电脑)  │                   └──────┬───────────┘
└─────────────┘                          │ iPad/Web/PC 协议
                                         ▼
┌───────────────────────────────────────────────────┐
│  dsh-wechat 插件（运行在 DSH 进程内，Cordis 插件）   │
│                                                   │
│  ┌──────────────┐ ┌───────────────┐ ┌──────────┐  │
│  │ WechatyChannel│ │ SessionBridge │ │ Notifier │  │
│  │ puppet 适配   │ │ 指令路由+会话桥│ │ 事件→推送 │  │
│  │ 登录态管理    │ └──────┬───────┘ └────┬─────┘  │
│  └──────────────┘        │              │        │
│  ┌───────────────────────┴──────────────┴───────┐  │
│  │         DSH 运行时（ctx services）            │  │
│  │  ctx.agents / sessions / workspaces / tools  │  │
│  └──────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────┘
```

与公众号版架构完全同构——**只有 Channel 层不同**，这保证了以后想加公众号/企业微信通道时 Bridge/Notifier 零改动。

### 模块说明

**① WechatyChannel**
- 封装 wechaty 实例生命周期：`scan`（出二维码）→ `login` → `message` → `logout`；
- 登录态持久化：memory card / token 存 `~/.dsh/storages/dsh-wechat/`，重启自动恢复；
- 掉线重连 + 心跳监控，掉线时在 DSH GUI 插件页提示重新扫码；
- 消息收发：文本、图片、文件、语音（语音 V4 转文字）；
- 出口限流：个人号发消息过快容易触发风控，内置发送队列（如 ≥1s/条，随机抖动）。

**② SessionBridge（核心）**
- 身份模型：登录的 Bot 账号即身份主体。**指令来源过滤**：
  - 只响应 `文件传输助手(filehelper)`、用户自己（self）、白名单 wxid 的私聊、白名单群里的 @消息；
  - 其他一切消息直接忽略（防 Bot 被他人劫持下指令——个人号方案的头号安全问题）；
- 维护 `聊天窗口(talker/room) ↔ DSH profile ↔ 当前活跃会话` 映射（存 `bindings.json`）——**不同聊天窗口可绑定不同会话**，比如文件传输助手绑工作区 A，某群绑工作区 B；
- 指令集（`/` 前缀，其余文本直接当 prompt）：
  - `/ws` — 列出所有工作区
  - `/sessions` — 列出当前工作区会话（带编号）
  - `/use 3` — 切换活跃会话
  - `/new 帮我写个脚本` — 新建会话
  - `/status` — 当前会话运行状态
  - `/log [n]` — 拉取最近 n 条输出
  - `/approve` `/reject` — 响应待审批请求
  - `/stop` — 中断当前会话执行
  - `/help` — 帮助

**③ Notifier**
- 订阅 DSH 事件：会话完成、approval 请求、cron 结果、长任务心跳；
- 推送到该会话绑定的聊天窗口：
  - 完成：*"✅ [会话xxx] 完成：已翻译 README.md（+312 行），回复 /log 看全文"*；
  - 审批：*"⚠️ [会话xxx] 请求执行 `rm -rf ./tmp`，回复 /approve 或 /reject"*；
  - 可选流式进度：每 N 秒合并为一条（默认关）；
- 长输出：单条 ≤ 2000 字节自动分段；超长转 H5 链接（V3）或直接发 txt 文件（个人号可以发文件，比公众号还方便）。

---

## 4. 核心流程

### 4.1 扫码上线

```
插件启动 → 无有效登录态 → scan 事件 → GUI 插件页显示二维码（终端同步显示）
用户手机微信扫码 → 确认登录 → login 事件
   → 缓存登录态 → 给"文件传输助手"发一条: "🤖 dsh-wechat 已上线，发 /help 查看指令"
之后: 重启 DSH → 自动恢复登录态，免扫码；掉线 → GUI 提示重扫
```

### 4.2 日常操控（文件传输助手场景）

```
用户在手机微信"文件传输助手"里:
  "/sessions"        → 回复会话列表（编号+标题+状态）
  "/use 2"           → "已切换到会话: 修复登录bug"
  "把 README 翻译成英文"
                     → "🚀 已发送给会话[修复登录bug]，完成后通知你"
                     → …DSH 后台执行…
                     → "✅ 完成：已翻译 README.md，回复 /log 查看"
```

### 4.3 远程审批

```
DSH 触发 approval(ask) → Notifier 拦截 → 推送到绑定窗口:
   "⚠️ [会话xxx] 请求执行: bash -c "rm -rf ./tmp"
    回复 /approve 批准，/reject 拒绝，/log 看上下文"
用户回复 /approve
   → 插件校验: 消息来源 ∈ {filehelper, self, 白名单} 且该窗口绑定了此会话
   → 写回 DSH approval answerer → "✅ 已批准，继续执行"
超时: 5 分钟未响应按配置 fallback（默认保持 ask，GUI 仍可处理）
```

### 4.4 多窗口 ↔ 多工作区（个人号独有玩法）

```
窗口绑定:  在任意私聊/群里发 "/bind <workspace名>"
           → 该窗口后续消息都路由到这个工作区的活跃会话
典型用法:  文件传输助手 → 日常工作区
           "AI实验"群    → 实验项目工作区（群里 @Bot 下指令，成员围观执行过程）
```

---

## 5. DSH 侧实现要点

DSH 插件是 Cordis 插件：导出 `apply(ctx)`，`dsh plugin add` 安装为 profile bundle。

### 5.1 插件骨架

```
dsh-wechat/
├── package.json          # 声明 dsh.bundle.patch; deps: wechaty, puppet-*
├── cordis.patch.yml
├── src/
│   ├── index.ts          # apply(ctx) 入口
│   ├── config.ts         # Cordis Schema 配置
│   ├── channel.ts        # WechatyChannel：puppet 生命周期 + 消息收发 + 限流
│   ├── bridge.ts         # SessionBridge：指令路由 + 窗口↔会话映射
│   ├── notifier.ts       # 事件 → 微信推送
│   ├── auth.ts           # 来源过滤（filehelper/self/白名单）+ 窗口绑定
│   └── qrcode.ts         # 二维码渲染（GUI 页面 + 终端）
└── client/               # V3: 移动端 H5（复用 DSH Web API，个人号扫码登录态无关，走 token）
```

```ts
// src/index.ts
import type { Context } from '@deepseek-ai/cordis'
import { WechatyChannel } from './channel'
import { SessionBridge } from './bridge'
import { Notifier } from './notifier'

export const name = 'dsh-wechat'

export function apply(ctx: Context) {
  const config = ctx.config   // puppet, token, allowlist, rateLimit...
  const channel = new WechatyChannel(config)
  ctx.plugin(channel)                        // wechaty 生命周期随插件热重载
  ctx.plugin(new SessionBridge(ctx, channel))
  ctx.plugin(new Notifier(ctx, channel))
}
```

```ts
// src/channel.ts（核心骨架）
import { WechatyBuilder, ScanStatus } from 'wechaty'

export class WechatyChannel {
  async start() {
    this.bot = WechatyBuilder.build({
      name: 'dsh-wechat',
      puppet: this.config.puppet,               // 'wechaty-puppet-padlocal' 等
      puppetOptions: { token: this.config.token },
    })
    this.bot
      .on('scan',  (qrcode, status) => this.emitQr(qrcode, status))  // → GUI/终端
      .on('login', (user) => this.onLogin(user))                     // 缓存登录态
      .on('message', (msg) => this.queueInbound(msg))                // → SessionBridge
      .on('logout', () => this.onLogout())                           // GUI 提示重扫
    await this.bot.start()
  }
  async say(target, text) { /* 限流队列：≥1s/条 + 随机抖动，长文本自动分段 */ }
}
```

### 5.2 依赖的 DSH 能力

| 能力 | 用途 | 接入方式 |
|---|---|---|
| 会话/工作区枚举 | `/sessions`、`/ws` | ctx 的 agents/sessions 服务（与 Web GUI 同源） |
| 向会话发消息 | 用户文本 → prompt | 复用 GUI 发送路径对应的 runtime API |
| 会话事件订阅 | 输出回流、完成通知 | Cordis 事件系统 |
| Approval 拦截 | 远程审批 | 注册插件级 answerer，转发微信、决策写回 |
| 插件配置 | puppet/token/白名单 | Cordis Schema + `settings.yaml`，GUI 设置页可编辑 |
| GUI 插件页 | 登录二维码展示、在线状态、绑定管理 | client-plugin（挂到设置页） |

### 5.3 配置示例

```yaml
plugins:
  dsh-wechat:
    puppet: wechaty-puppet-padlocal   # 或 wechaty-puppet-wechat4u / wechaty-puppet-xp
    puppetToken: xxxxxxxx             # padlocal 等付费 puppet 的 token
    security:
      listenFilehelper: true          # 响应文件传输助手（默认开）
      listenSelf: true                # 响应自己发给自己的消息
      allowContacts: []               # 额外信任的私聊 wxid 白名单
      allowRooms: []                  # 信任的群（需 @Bot 才响应）
      dangerousOpsNeedApproval: true
    push:
      onSessionComplete: true
      onApproval: true
      streamProgress: false           # 流式进度（合并节流）
      progressIntervalSec: 10
    reply:
      maxChunkBytes: 2000
      rateLimitMs: 1200               # 发送节流，防风控
```

---

## 6. 安全设计（个人号方案的重中之重）

1. **来源过滤是第一道命**：默认**只**响应文件传输助手 + 自己 + 显式白名单。任何人把 Bot 拉进群、加 Bot 好友发消息，一律静默忽略并记日志。
2. **风控自保**（防封号）：
   - 发送限流（≥1.2s/条 + 随机抖动），长回复分段间隔发；
   - 不主动加好友、不主动拉群、不群发——Bot 只"被动应答 + 向已绑定窗口推送"；
   - 推送合并节流，避免短时间大量消息；
   - 登录态失效后**不自动反复重试登录**（频繁扫码登录是风控高危行为），转为 GUI 提示人工扫码。
3. **指令权限**：
   - 消息只注入该窗口绑定的工作区/会话；
   - `/approve` 校验窗口↔会话归属，群场景下只认发指令人 ∈ 白名单；
   - 危险操作不绕过 DSH 原有 approval，微信只是多一个审批入口。
4. **审计**：登录/掉线、每条入站指令、每条审批决策写 `~/.dsh/storages/dsh-wechat/audit.jsonl`。
5. **凭据安全**：puppet token、登录态缓存只存 `~/.dsh/storages/`，权限 600，不进 git。

---

## 7. 部署形态

| 场景 | 说明 |
|---|---|
| DSH 跑在本机（Mac/Linux） | 插件随 DSH 进程跑，padlocal/wechat4u 均可，零额外部署 |
| DSH 跑在服务器 | 同样零额外部署（协议登录不依赖本地微信客户端），GUI 插件页远程展示二维码扫码即可 |
| DSH 跑在 Windows | 可额外选 puppet-xp（免费，但锁 PC 微信版本） |

个人号方案**不需要公网回调、不需要域名证书**——Bot 是主动长连接微信服务器，这是相比公众号方案在部署上的巨大优势。

---

## 8. 分期路线图

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| **MVP（1-2 周）** | wechaty 接入（padlocal + wechat4u 双 puppet）、扫码登录（GUI+终端）、来源过滤、`/ws /sessions /use /new /status /log /stop /help`、prompt 转发、完成推送 | 手机微信文件传输助手里从零跑通一个完整任务 |
| **V2（1 周）** | `/approve /reject` 远程审批、窗口↔工作区绑定（`/bind`）、审计日志、发送限流打磨 | 在外用微信批准一次 shell 操作 |
| **V3（2 周）** | 超长输出发 txt 文件、图片消息入站（截图→会话）、群模式（@Bot）、在线状态 GUI 页 | 群里 @Bot 跑任务，成员围观 |
| **V4（按需）** | 语音转 prompt、puppet-xp/gewechat 适配、掉线自动告警（邮件/TG 备用通道） | — |

---

## 9. 风险清单（务必知悉）

| 风险 | 等级 | 缓解 |
|---|---|---|
| **Bot 账号被限制/封号** | 🔴 高 | 用小号；发送限流；不加好友不拉群不群发；不频繁重登；接受"号没了换号"的运维成本 |
| puppet 项目失效/停更 | 🟡 中 | puppet 配置化隔离，多 puppet 备选；关注 wechaty 社区 |
| 他人劫持 Bot 下指令 | 🟡 中 | 默认只响应 filehelper/self/白名单；全量审计日志 |
| 长任务输出含敏感信息推送到微信 | 🟡 中 | 推送脱敏开关；超长转本地 H5 而非微信消息 |
| padlocal token 费用 | 🟢 低 | wechat4u 免费备选（挑号）；xp（Windows）免费 |
| DSH 升级 API 变动 | 🟢 低 | 声明兼容版本范围，CI smoke test |

---

## 10. 一句话总结

**wechaty + 可插拔 puppet（默认 padlocal）做协议层，扫码登录一次长期在线，"文件传输助手"就是 DSH 的遥控器，`/bind` 让每个聊天窗口绑定不同工作区，approval 推送到微信一键放行**——零公众号资质、零公网部署、交互全自由，代价是接受个人号协议方案的封号风险并严格用小号 + 限流自保。
