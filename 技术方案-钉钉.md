# 方案：接入钉钉通道（dsh-chatops × 钉钉）

> 结论：**可行，与飞书同构**。钉钉开放平台企业内部应用 + 机器人 + Stream 长连接（官方 `dingtalk-stream` SDK），参考实现 xmanrui/dsh-im（MIT）已确认全部要点。

---

## 1. 接入方式（钉钉官方能力）

| 项 | 方案 | 说明 |
|---|---|---|
| 应用形态 | **企业内部应用 + 机器人能力** | 钉钉开放平台创建；需一个钉钉组织（个人可免费创建团队，无需企业认证即可自用） |
| 凭据 | **Client ID + Client Secret**（即 AppKey/AppSecret） | 手动填入插件配置；dsh-im 的扫码创建（device-auth）可作为后续增强 |
| 收消息 | **Stream 模式长连接**（`dingtalk-stream` SDK 的 `DWClient`，topic `TOPIC_ROBOT`） | 主动出站 WebSocket，无需公网回调/域名；SDK 负责 access_token 管理 |
| 发消息 | 机器人单聊/群聊消息 API（`api.dingtalk.com`） | 文本/Markdown/卡片 |
| 群聊 | 拉机器人入群，@触发 | 与飞书同一交互模型 |
| 审批 | **互动卡片 + 按钮回调**（Stream 模式可收卡片回调 topic） | 与飞书审批卡同体验 |
| 流式进度 | **AI Card 流式更新**（钉钉专有 API） | dsh-im 已实现（dingtalk-card-stream.mjs），可移植 |
| 文件 | 媒体上传 → 文件消息 | 钉钉媒体上传接口 + 文件消息类型 |

## 2. 架构改动（极小）

现有分层全部复用，新增一个通道目录：

```
src/host/dingtalk/
├─ channel.ts    DingTalkChannel：DWClient 长连接、事件归一化、发送（复用队列语义）
└─ cards.ts      审批互动卡 / AI Card 流式（二期）

改动点：
├─ manager.ts     注册前缀 dsu:{userId}（私聊）/ dsc:{conversationId}（群）
├─ auth.ts        前缀表加 dsu:/dsc:
├─ index.ts       通道工厂加 'dingtalk' 分支 + 卡片回调接 bridge.decideApprovalById
├─ config.ts      通道枚举加 'dingtalk' + dingtalk: { clientId, clientSecret }
└─ package.json   optionalDependencies 加 dingtalk-stream（懒加载）
```

Bridge / SessionBridge / 审批 answerer / 文件回传工具 **零改动**——`/send`、`im_send_file` 通过能力探测自动支持钉钉（`sendFile` 方法同接口）。

## 3. 用户侧准备（约 10 分钟）

1. 钉钉开放平台（open-dev.dingtalk.com）→ 创建组织（个人可免费建团队）→ 创建**企业内部应用**；
2. 添加**机器人**能力，消息接收模式选 **Stream 模式**；
3. 记录 **Client ID / Client Secret**；
4. 权限：机器人消息收发相关权限（`qyapi_robot_sendmsg` 等，以控制台提示为准）；
5. 版本发布（自用范围选自己）。

## 4. 与其他通道的能力对照

| | 微信 iLink | 飞书 | 钉钉 |
|---|---|---|---|
| 合规 | 官方 Bot 平台 | 官方开放平台 | 官方开放平台 |
| 凭据 | 扫码绑定 | AppID/Secret | ClientID/Secret（可升级扫码创建） |
| 传输 | 35s 长轮询 | WS 长连接 | Stream 长连接 |
| 审批 | 文字指令 | 卡片按钮 | 卡片按钮 |
| 流式进度 | 无 | 卡片原地更新 | AI Card 流式 |
| 文件回传 | ✅ 已实现 | ✅ 已实现 | 媒体上传（同模式） |
| 组织门槛 | 无 | 免费团队 | 免费团队 |

## 5. 工作量与建议

- **约 1-1.5 天**（文本链路 0.5 天可跑通：Stream 连接 + 私聊收发 + 指令复用；审批卡 + 群@ + 文件再 0.5-1 天）；
- 建议在飞书通道真机验收通过后开工——飞书暴露的问题（SDK 行为、卡片回调字段）在钉钉上会再遇到一遍，先趟一遍坑能省返工；
- 三通道并行已就绪：`channels: [ilink, feishu, dingtalk]`。

## 6. 风险

- 钉钉 Stream SDK 的事件/卡片回调字段需真机校准（与飞书同理）；
- AI Card 流式是独立 API，复杂度高于飞书卡片 patch，放二期；
- 钉钉组织的应用发布在个别企业模板下可能要求管理员审批——自建团队无此问题。
