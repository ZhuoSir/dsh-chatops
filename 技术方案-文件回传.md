# 方案：DSH 主动给微信发送文件（文件回传）

> 结论：**能实现**，iLink 协议原生支持文件/图片消息。dsh-im 已验证全链路（其"结果文件回传"功能），协议细节已从源码确认，可直接移植。
> 典型场景：你对手机微信说"把分析结果整理成报告发给我"→ DSH 生成 md/pdf/csv → 模型调用回传工具 → 微信收到原生文件消息，点开即看。

---

## 1. iLink 文件发送协议（已从 dsh-im 源码确认）

四步，全部 HTTPS + AES，纯 Node 内置模块可实现（`node:crypto` + fetch）：

```
① POST {baseUrl}/ilink/bot/getuploadurl
   body: { filekey: <16字节随机hex>, media_type: 3(文件)/1(图片),
           to_user_id, rawsize: <原始字节数>, rawfilemd5: <md5 hex>,
           filesize: <AES-ECB 填充后尺寸>, no_need_thumb: true,
           aeskey: <16字节随机密钥hex>, base_info }
   → 返回 { upload_full_url } 或 { upload_param }

② 加密：AES-128-ECB(aesKey, 文件字节) → ciphertext

③ POST 上传（application/octet-stream，60s 超时，最多重试 3 次）
   url = upload_full_url
         或 https://novac2c.cdn.weixin.qq.com/c2c/upload?encrypted_query_param={upload_param}&filekey={filekey}
   → 响应头 x-encrypted-param = downloadParam（下载凭证）

④ POST {baseUrl}/ilink/bot/sendmessage
   item_list: 文件: [{ type: 4, file_item: { media, file_name, len: String(rawsize) } }]
             图片: [{ type: 2, image_item: { media, mid_size: ciphertextSize } }]
   其中 media = { encrypt_query_param: downloadParam,
                  aes_key: base64(hex编码的aesKey), encrypt_type: 1 }
```

图片自动识别：按扩展名（jpg/jpeg/png/webp/gif）走 `image_item`（微信里直接显示图），其余走 `file_item`（文件卡片）。

## 2. 触发方式设计（三种，建议全做）

### ① 模型主动回传（核心，"DSH 主动发"的正解）

注册一个插件 Tool（`ctx.tools.register`，与 dsh-cron 注册 cron_add 同一机制）：

```
工具名: im_send_file
参数: { path: string（工作区内文件路径）, to?: string（默认当前会话绑定的微信窗口）, caption?: string }
```

模型在会话里可自行调用——你说"做完发我微信"，它就调用此工具。工具实现：
1. 校验 path 存在、可读、**必须在当前会话工作区内**（防任意文件外泄）；
2. 找到当前会话绑定的微信窗口（AuthStore 反查）；
3. 调通道的 `sendFile` / `sendImage`；
4. 返回发送结果给模型（成功/平台拒绝原因）。

### ② 用户命令：`/send <路径>`

微信里手动发 `/send reports/weekly.md` → 当前绑定会话工作区里的该文件直接回传。

### ③ 超长输出自动转文件（顺手解决上次的痛点）

`/log` 内容超过阈值（如 4000 字符）时，不再拆十几条消息，而是生成 `.txt` 直接发文件——比刷屏优雅得多。配置开关 `push.longOutputAsFile: true`。

## 3. 实现模块（落在现有架构上）

| 文件 | 改动 | 内容 |
|---|---|---|
| `src/host/ilink/api.ts` | 加 ~150 行 | `getUploadUrl` / `uploadCdn`（AES-128-ECB + MD5 + 重试）/ `sendFileItem`（type 4）/ `sendImageItem`（type 2）——从 dsh-im 移植 |
| `src/host/ilink/channel.ts` | 加方法 | `sendFile(windowKey, {filePath, fileName})`、`sendImage(windowKey, ...)`；接入现有发送队列 |
| `src/host/bridge.ts` | 加命令 | `/send <path>` 解析 + 工作区边界校验 |
| `src/host/index.ts` | 注册工具 | `ctx.tools.register`（inject 加 `'tools'`），工具回调路由到当前会话绑定窗口 |
| `manager.ts` | 加能力探测 | `fileCapable(windowKey)` 结构探测（飞书通道后续补同接口） |

## 4. 安全边界（重点）

- **路径围栏**：只允许发送当前会话工作区目录内的文件（resolve 后 startsWith 校验），拒绝 `..` 逃逸和绝对路径越界；
- **接收方围栏**：文件只发到"绑定了该会话"的微信窗口，旁人无法诱导模型发文件到自己微信；
- **审计**：每次发送写 audit.jsonl（文件路径、大小、目标窗口、触发者）；
- **大小护栏**：默认上限 20MB 可配置（`reply.maxFileMB`），超限明确提示而不是静默失败；
- 平台侧仍可能按账号等级/配额拒绝，此时把平台返回的原因如实回给模型/用户。

## 5. 风险与未知点

| 风险 | 说明 | 对策 |
|---|---|---|
| 平台文件大小/类型限制 | iLink 未公开文档，dsh-im 也未设限，"以接口返回为准" | 失败时透传平台错误；先做常见格式（txt/md/csv/pdf/zip/图片）实测 |
| AES-ECB 细节 | 无 IV、PKCS7 填充、密钥 hex→base64 的两次编码容易写错 | 严格照 dsh-im 实现移植 + 用真实小文件发收对比验证 |
| 飞书通道文件发送 | 飞书是另一套 API（im.file.create 上传 + 文件消息） | 二期补，通道能力探测已留口 |

## 6. 工作量

约 **1 天**：协议移植 + 通道方法 0.5 天，工具注册 + /send 命令 + 超长转文件 0.5 天，真机验证（发 txt/md/png 各一）收尾。
