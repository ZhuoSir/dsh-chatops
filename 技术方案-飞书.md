# dsh-wechat × 飞书互通实施计划

> 目标：与微信通道相同的效果——飞书里加一个机器人，私聊/群@它即可操控 DSH 全部工作区和会话，任务完成自动推送，危险操作远程审批。
> 飞书走**官方开放平台自建应用**，合规、能力比微信 iLink 更强（交互卡片、按钮回调、流式更新）。

---

## 0. 为什么这个计划很短

现有插件是分层架构，**90% 代码直接复用**：

```
FeishuChannel（新写）  ← 唯一要写的通道层
SessionBridge          ← 零改动（windowKey 命名空间天然隔离：user:{open_id} / chat:{chat_id}）
AuthStore              ← 零改动（owner/白名单模型照用）
审批 answerer          ← 小改：从"/approve 指令"升级为"卡片按钮回调"
```

协议参考：[xmanrui/dsh-im](https://github.com/xmanrui/dsh-im) 飞书渠道（MIT，已确认实现细节）。

## 1. 通道选型（飞书官方能力）

| 能力 | 方案 | 说明 |
|---|---|---|
| 应用形态 | **企业自建应用 + 机器人能力** | 需要飞书租户（个人可免费创建团队）；开发者后台创建应用，拿 App ID / App Secret |
| 收消息 | **WebSocket 长连接**（`lark-oapi` SDK 的 `WSClient`） | 无需公网回调/域名/端口转发，和 iLink 长轮询一样主动出站 |
| 发消息 | `im/v1/messages` REST（SDK 封装） | tenant_access_token 由 SDK 自动管理刷新 |
| 事件订阅 | `im.message.receive_v1`（消息）+ `card.action.trigger`（卡片按钮） | 都在长连接上推送 |
| 杀手级能力 | **交互卡片 + 按钮回调** | 审批推送一张卡片，用户点【批准】/【拒绝】按钮直接回传——比微信的 /approve 指令体验好一档 |
| 进阶能力 | **卡片原地更新（流式）** | 一张卡片从"🔄 执行中…"原地刷成最终结果——微信做不到，V2 实现 |

依赖：`@larksuiteoapi/node-sdk`（官方 SDK，纯 JS）。

## 2. 接入准备（用户侧一次性操作，约 10 分钟）

1. 打开 [飞书开发者后台](https://open.feishu.cn/app)（没有租户就先免费创建一个团队）；
2. 创建**企业自建应用** → 添加**机器人**能力；
3. 权限配置：`im:message`、`im:message:send_as_bot`（发消息）、`im:resource`（图片/文件，二期）；
4. 事件订阅：订阅方式选**使用长连接接收事件**，添加 `接收消息 im.message.receive_v1` 和 `卡片回传交互 card.action.trigger`；
5. 创建版本并发布（自建应用仅自己用时，可用范围选自己即可）；
6. 把 **App ID / App Secret** 填进插件配置（或 GUI 设置页）。

> V2 可做 dsh-im 那样的"扫码一键创建机器人"（参考其 `registration-manager.mjs`），MVP 先手动填凭据。

## 3. 架构与模块

```
飞书用户 ──私聊/群@──► 飞书开放平台
                          ▲ WSClient 长连接（主动出站，零公网部署）
              dsh-wechat 插件 (Cordis, DSH 进程内)
              ├─ FeishuChannel（新）  WSClient + EventDispatcher + REST 发送
              ├─ SessionBridge        复用：/sessions /use /status /log /approve …
              ├─ AuthStore            复用：owner = 第一个私聊 open_id / 配置指定
              └─ Notifier             复用 + 升级：审批改发交互卡片
```

**新增文件**：

| 文件 | 职责 | 工作量 |
|---|---|---|
| `src/host/feishu/channel.ts` | 生命周期（凭据校验 → WSClient 连接 → 事件分发）、入站归一化为 `InboundMessage`、出站发送（复用双通道队列语义）、断线重连（SDK 自带） | 1 天 |
| `src/host/feishu/cards.ts` | 审批卡片 JSON、按钮回调 → 映射为 `/approve` `/reject` 决策 | 0.5 天 |
| `src/host/index.ts` | 通道工厂加 `feishu` 分支 | 0.1 天 |
| `config.ts` / `cordis.patch.yml` | `feishu: { appId, appSecret, domain(feishu/lark) }` | 0.1 天 |

**入站映射**：
- 私聊：`windowKey = user:{open_id}`，`chat_type=p2p` 直接响应；
- 群聊：`windowKey = chat:{chat_id}`，仅当被 @（event 里 `mentions` 含机器人）才响应，talker 取 `sender.sender_id.open_id` 过白名单；
- 文本提取：`message.content` JSON 的 `text` 字段（post 富文本二期）。

**出站**：`im.message.reply`（带消息上下文）或 `create`；长文本复用 `chunkText`（飞书单条上限比微信宽，默认 6000 字节沿用）。

## 4. 审批升级（飞书独有）

```
DSH 触发 approval → Notifier 发交互卡片:
┌─────────────────────────────┐
│ ⚠️ 审批请求                  │
│ 会话: xxx                    │
│ 操作: bash -c "rm -rf ./tmp" │
│ [ ✅ 批准 ]   [ ❌ 拒绝 ]     │
└─────────────────────────────┘
用户点按钮 → card.action.trigger 回调（含 operator open_id）
→ 校验操作者 ∈ 白名单 → 写回 DSH approval answerer
→ 原地更新卡片为"✅ 已批准 by xxx"（update message）
```

兜底保留 `/approve` `/reject` 文字指令（卡片回调未配置时可用）。

## 5. 分阶段计划

| 阶段 | 内容 | 工作量 | 验收 |
|---|---|---|---|
| **P0** | 用户建租户+应用+权限+事件订阅，拿到凭据 | 用户 10 分钟 | 凭据可用 |
| **P1 MVP** | FeishuChannel：凭据配置、WS 长连接、私聊文本收发、指令集、prompt 转发、完成推送、双通道队列 | 1-1.5 天 | 飞书私聊跑通完整任务 |
| **P2** | 审批交互卡片（按钮回调）、群@模式、owner/白名单 | 1 天 | 飞书里点按钮批准 shell 操作 |
| **P3** | 流式进度卡片（原地更新）、图片/文件收发、扫码一键创建机器人、多通道并行（微信+飞书同时在线） | 1-2 天 | — |

**总计约 3-4 天，MVP 1.5 天内可用。**

## 6. 风险与注意

- **租户门槛**：需要飞书团队（免费创建）；企业租户里自建应用发布可能需要管理员审批——用自己的团队最省事；
- **SDK 依赖**：`@larksuiteoapi/node-sdk` 体积不小，作为 optionalDependency 懒加载（没装时插件照常加载并给安装指引），与 wechaty 同一策略；
- **多通道并行**（P3）：当前 `config.channel` 是单选，并行需把通道实例化改为数组；Bridge/Auth 已是多通道安全（windowKey 命名空间隔离），改动集中在 index.ts；
- ** Lark 海外版**：domain 配置项区分 `feishu`（国内）/ `lark`（海外）；
- **卡片回调**需要在开发者后台订阅 `card.action.trigger`，dsh-im 的教训是这个配置容易漏——P2 验收时先做一次按钮实测（它甚至内置了 /repair 自检流程，可借鉴）。

## 7. 与微信通道的差异一览

| | 微信 ClawBot (iLink) | 飞书 |
|---|---|---|
| 合规 | 官方 Bot 平台 | 官方开放平台 |
| 绑定 | 扫码即得机器人 | 手动建应用填凭据（V2 扫码一键创建） |
| 传输 | 35s 长轮询 | WebSocket 长连接（更快） |
| 审批 | 文字指令 | **卡片按钮** |
| 进度 | 无 | **流式卡片原地更新** |
| 群聊 | 二期 | P2（@触发） |
| 文件/图片 | 二期（CDN+AES） | P3（SDK 封装好，更简单） |
